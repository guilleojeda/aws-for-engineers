{
  "name": "aws-for-engineers",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "prebuild": "node scripts/clean-output.mjs",
    "build": "eleventy",
    "dev": "eleventy --serve",
    "check": "node scripts/check-site.mjs"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.1.6"
  }
}
